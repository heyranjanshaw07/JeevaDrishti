import DetectionResults from './DetectionResults'

/**
 * AnalysisResults (Alias for DetectionResults for backward compatibility)
 */
export default function AnalysisResults(props) {
  return <DetectionResults {...props} />
}
